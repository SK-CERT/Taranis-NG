name: Docker image - GUI

on:
  push:
    paths:
      - 'src/gui/**'
      - 'docker/Dockerfile.gui'
      - 'VERSION.md'
    branches:
      - main

  workflow_run:
    workflows: ["Update git_version.txt"]  # must match your version_file.yaml name
    types:
      - completed

permissions:
  packages: write

jobs:
  build:
    name: build docker
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Get GHCR owner repository
        run: |
          echo "GHCR_OWNER=${GITHUB_REPOSITORY_OWNER,,}" >>${GITHUB_ENV}

      - name: Build and push GUI
        uses: docker/build-push-action@v6
        with:
          file: docker/Dockerfile.gui
          push: true
          tags: |
            ghcr.io/${{ env.GHCR_OWNER }}/taranis-ng-gui:${{ github.ref_name }}
