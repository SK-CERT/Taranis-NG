# Ansible deployment

The Ansible files in this directory are not a supported installation method.
They target unsupported operating-system releases and depend on external inputs
that are not part of this repository.

Use the [Docker deployment guide](../docker/README.md) for installation,
configuration, upgrades, and operational verification. Do not use these
playbooks for production or evaluation deployments.
